package com.example.githubapp.business.repositories

import com.example.githubapp.models.Repository
import com.example.githubapp.repositories.repositories.RepositoriesRepository

class RepositoryInteractor(repositoriesRepository: RepositoriesRepository) {
    fun saveRepository(repository: Repository) {
        TODO("Not yet implemented")
    }
}